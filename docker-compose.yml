services:
  api:
    build: .
    image: diabetes-api:latest
    container_name: diabetes-api
    ports:
      - "8000:8000"
    volumes:
      - ./conf:/app/conf:ro
      - ./data:/app/data
    environment:
      - DO_NOT_TRACK=1
    restart: unless-stopped
